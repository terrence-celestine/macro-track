# Revisions to skip when running `git blame`.
#
# Bulk reformats only — commits that changed how the code looks and nothing
# about what it does. Without this, blame on almost every line points here
# instead of at the change that actually introduced the logic.
#
# GitHub honours this file automatically. Locally, opt in once with:
#
#   git config blame.ignoreRevsFile .git-blame-ignore-revs
#
# Hashes must be the full 40 characters — a short hash or one git cannot find
# makes `git blame` fail outright rather than degrade.
#
# To add a future one: commit the reformat on its own, then
#
#   git rev-parse HEAD
#
# and paste the result here in a *separate* follow-up commit. Amending the
# reformat to include this file would change its hash and invalidate the line
# you just wrote.

# fix: Added prettier and formatted all files
8d64747b61d945cc730878a576dda627c2234266
